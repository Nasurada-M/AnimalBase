// Top-level build file — no Firebase, no google-services plugin
plugins {
    id 'com.android.application' version '8.1.4' apply false
    id 'org.jetbrains.kotlin.android' version '1.9.10' apply false
    // google-services removed — we use WebSocket + PostgreSQL for notifications
}
